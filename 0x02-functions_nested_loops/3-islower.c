#include <stdio.h>
#include "main.h"
#include <ctype.h>

/**
*
* main - Entry Point
*
* Return: Always 0 (Success)
*/
int main(void)
{
int _islower(int ch);
char ch;
{
if (islower(c) == 1)
}
{
else (0)
}
}
	
