#include <stdio.h>
#include "main.h"

/**
* print_last_digit - digittttttttit
* @a: stststs
* Return: lastDigit
*/
int print_last_digit(int a)
{
int lastDigit(int a)
{
a = a % 10;
if (a <= 0)
{
a *= -1;
}
return (a);
}
}
