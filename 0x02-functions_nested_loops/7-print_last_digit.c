#include <stdio.h>
#include "main.h"

/**
* print_last_digit - digittttttttit
* @a: stststs
* Return: lastDigit
*/
int print_last_digit(int a)
{
int lastDigit(int a)
{
a = a % 10;
if (a < 0)
{
a = a * -1;
_putchar ('0' + a);
}
return (1);
}
}
