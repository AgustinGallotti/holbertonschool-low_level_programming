#include "main.h"

/**
* _strchr - character
* @s: se
* @c: cc
* Return: null or c
*/
char *_strchr(char *s, char c)
{
	int a, b;

	for (a = 0; s[a] != c; a++)
	{
	s[a] = c;
	return (c);
	}
		for (b = 0; s[b] = '\0';)
			return (0);
}
