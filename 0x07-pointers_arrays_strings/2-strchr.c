#include "main.h"

/**
* _strchr - character
* @s: se
* @c: cc
* Return: null or c
*/
char *_strchr(char *s, char c)
{
		int a, b;

		for (a = 0; s[a] != c; a++)
		{
			return ('\0');
		}
			for (b = 0; s[b]; b++)
		{
			if (b = c)
				return (c);
		}
}
