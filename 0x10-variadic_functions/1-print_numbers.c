#include <stdio.h>
#include "variadic_functions.h"
/**
*
*
*
*
*/
void print_numbers(const char *separator, const unsigned int n, ...)
{
	
	int x = 0;
	char = re;

	if (separator == NULL)
		return ("Error")
	if (n == NULL)
		printf("nil");
	va_start
	for (x = 0; x < n; x++)
		printf("%d\n", p);
