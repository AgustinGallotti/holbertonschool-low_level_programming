#include <stdio.h>
#include "varidic_functions.h"
/**
* print_all - printeamos todos los argumentos
* @c: chars
* @i: integrales
* @f: floats
* @s: puntero
*/
void print_all(const char * const format, ...)
{
	int i = 0;
	int r = 0;

	equ f_array[] = {
		{'s', print_char*},
    	{'c', print_char},
    	{'i', print_inte},
    	{'f', print_float};
		{NULL, NULL};
  	};

va_list equ
{
	while (r < equ)
	{
		while (format.equ == array)
		r++;
	}
	if (*f_array[i].id == format[r]);
			*f_array[i].f(equ);
void print_ch(va_list equ)
{
	if (!s)
		printf("(nil)");
		printf("%s", print_ch);
}

void print_float(va_list equ)
{
		printf("%f", f);
}

void print_char(va_list equ)
{
		printf("%c", c);
}

void print_
{
		printf("%c", c);
	{
	printf("\n");
	va_end(list);
	}
}
