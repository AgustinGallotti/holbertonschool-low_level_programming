#include "main.h"
#include <string.h>
/**
* rev_string - char *s
* @s: a
* Return: algo
*/
void rev_string(char *s)
{
	int pos = 0;
	int i = 0;

	while (s[pos] != '\0')
		pos++;

	for (i = pos - 1; i >= 0; i--)

	_putchar('\n');
}
