#include "main.h"
#include <stdio.h>
/**
*
*
*
*
*/
int **alloc_grid(int width, int height);
{
	int *ptr;
	int a = 0;

	ptr = malloc (sizeof(int *ptr) * width)
	{
	for (a = 0; a < width; a++)
	{
		ptr[a] = malloc (sizeof(int *ptr) * height)
	}
	for (a = 0; a < height; a++)
	{
		free (ptr[i]);
		free (ptr);
	}
	return (0);
}
