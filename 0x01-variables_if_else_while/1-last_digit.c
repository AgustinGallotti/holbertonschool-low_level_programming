#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - Entry Point
*
* Return: 0 (Success)
*
*/
int main(void)
{
		int n;

			srand(time(0));
				n = rand() - RAND_MAX / 2;
					{
					if (n > 5)
						(n / 5);
					return (n);
					}

					int lastDigit(int n)
					{
						return (n % 10);
					}
					int n = 98562;
					cout << firstDigit(n) << " "
					     << lastDigit(n) << endl;
					return (0);
                    }
}
© 2022 GitH
