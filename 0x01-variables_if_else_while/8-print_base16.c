#include <stdio.h>

/**
* main - Entry Point
*
* Return: Always 0 (Success)
*
*/
int main(void)
{
	char ch;
	int num;

	for (num = '0'; num = '9'; num++)
	for (ch = 'a'; ch = 'z'; ch++)
	{
		putchar(num ; ch);
	}
	putchar('\n');
	return (0);
}
