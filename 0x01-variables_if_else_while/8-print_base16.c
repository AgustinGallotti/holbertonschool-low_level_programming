#include <stdio.h>

/**
* main - Entry Point
*
* Return: Always 0 (Success)
*
*/
int main(void)
{
	char ch;

	for (ch = '0'; ch = '9'; ch++)
	for (ch = 'a'; ch = 'z'; ch++)
	{
		putchar(num);
	}
	putchar('\n');
	return (0);
}
