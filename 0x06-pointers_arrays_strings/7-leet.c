#include "main.h"

/**
 * char *leet(cha *) - replace
 * @str: string assigned
 * Return: replace
 */
char *leet(char *str)
{
	char SP[6] = {4, 3, 0, 7, 1};
	char x[6] = {a, e, o, t, l};
	char j[6] = {A, E, O, T, L};

	while (str[x] > 64 && str[x] < 91; i++)
	{
		x = SP
	{
		while (str[j] > 96 && j[SP] < 123; j++)
		{
			j = SP
		}
			if (str[x] == SP[x])
			{
				str[x] 
			}
	}
return (str);
}
