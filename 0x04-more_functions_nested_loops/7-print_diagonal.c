#include <stdio.h>
#include "main.h"

/**
* print_diagonal - as
* @n: sd
*
* Return: msdks
*/
void print_diagonal(int n)
{
int count;

	if (n >= 0)
	{
		for (count = 0; count < n; count++)
		{
			_putchar(' ');
			_putchar('\');
			_putchar('\n');
		}
	}
}
